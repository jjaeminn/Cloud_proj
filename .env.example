# 현자의 프록시 설정 — 이 파일을 .env 로 복사한 뒤 값을 채우세요.
# cp .env.example .env

# LLM 공급자 (기본: gemini)
LLM_PROVIDER=gemini

# Google AI Studio 무료 API 키 → https://aistudio.google.com/apikey
GEMINI_API_KEY=

# (선택) 모델 이름. 무료 티어 기본값이면 비워둬도 됩니다.
GEMINI_MODEL=gemini-2.5-flash

# (선택) 프록시 포트
PORT=8787
